import 'package:flutter/material.dart';

class AppColors {
  // CareFlow Design System Colors
  static const Color primary = Color(0xFF2563EB); // Primary Blue
  static const Color primaryLight = Color(0xFFEFF6FF);

  static const Color secondary = Color(0xFF7C5CFC); // Secondary Lavender
  static const Color secondaryLight = Color(0xFFF3F0FF);

  static const Color success = Color(0xFF10B981); // Success Green
  static const Color successLight = Color(0xFFECFDF5);

  static const Color warning = Color(0xFFF59E0B); // Warning Amber
  static const Color warningLight = Color(0xFFFEF3C7);

  static const Color danger = Color(0xFFEF4444); // Danger Red
  static const Color dangerLight = Color(0xFFFEE2E2);

  static const Color emergency = Color(0xFFDC2626); // Emergency Red
  static const Color emergencyLight = Color(0xFFFEE2E2);

  static const Color background = Color(0xFFF7F9FC);
  static const Color card = Color(0xFFFFFFFF);

  static const Color mainText = Color(0xFF172554);
  static const Color secondaryText = Color(0xFF64748B);
  static const Color border = Color(0xFFE2E8F0);

  // Backward-compatible aliases
  static const Color white = Colors.white;
  static const Color black = Color(0xFF172554);
  static const Color grey = Color(0xFF64748B);
  static const Color error = Color(0xFFEF4444);
}